export enum ButtonText {
  VIEW = 'Edit',
  ADD = 'Save',
  EDIT = 'Update',
}

export enum TypeText {
  ADD = 'add',
  EDIT = 'edit',
  VIEW = 'view',
}

export enum MutationStatus {
  ERROR = 'error',
  IDLE = 'idle',
  PENDING = 'pending',
  SUCCESS = 'success',
}
